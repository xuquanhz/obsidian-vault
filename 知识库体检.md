---
title: 知识库体检
updated: 2026-09-15
tags: [规范, 体检, Dataview]
status: active
---

# 🩺 知识库体检

> 规范 §6 的体检项在这里落地。Dataview 出即时报表，深度报告跑 `工具/知识库体检.py`。

## 一键深度体检

在库根目录执行：

```bash
python 工具/知识库体检.py
```

脚本会重写 `工具/体检报告.md`，覆盖：断链、缺 frontmatter、空笔记、孤儿笔记、超期未更新、织网率。

---

## ① 缺 frontmatter 字段

```dataview
TABLE WITHOUT ID file.link AS "条目", updated AS "updated", status AS "status", tags AS "tags"
FROM ""
WHERE !updated OR !status
SORT file.folder ASC
LIMIT 50
```

## ② 空笔记 / 极短笔记

```dataview
LIST
FROM ""
WHERE file.size < 120
SORT file.size ASC
LIMIT 30
```

## ③ 孤儿笔记（无入链也无出链）

```dataview
TABLE WITHOUT ID file.link AS "条目", file.folder AS "位置"
FROM ""
WHERE length(file.inlinks) = 0 AND length(file.outlinks) = 0
SORT file.folder ASC
LIMIT 40
```

## ④ 待复核：过期与归档

```dataview
TABLE WITHOUT ID file.link AS "条目", updated AS "更新日", status AS "状态"
FROM ""
WHERE status = "deprecated" OR status = "archived"
SORT updated ASC
```

---

## 体检标准（达标线）

| 指标 | 达标线 | 说明 |
|:--|:--|:--|
| 断链 | 0 条 | 任何断链都要当场修或删链接 |
| frontmatter 覆盖率 | ≥ 95% | title / updated / tags / status 齐全 |
| 空笔记 | 0 篇 | 空文件直接删 |
| 织网率 | ≥ 20% 且逐季提升 | 有互链的笔记占比 |
| 收件箱堆积 | ≤ 7 天 | 超期即销毁判断 |

---

*规范依据：[[白水知识库管理规范.md]] ｜ 看板：[[工作台.md]]*
