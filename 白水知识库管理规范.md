---
title: 白水知识库管理规范
updated: 2026-09-15
tags: [规范, 知识库管理]
status: active
---

# 白水知识库管理规范

> 适用范围：`D:\白水知识库`（Obsidian 个人 / 工作 Wiki 知识库）
> 版本：v2.0 ｜ 生效日期：2026-09-15（v1.0 生效于 2026-08-31）
> 定位：基于"六维管理框架"，将知识库从"能用"提升到"管得住、可追溯、不过时"。
> **v2.0 变更**：新增"最小闭环工作流"（§0.1），把 采集 → 处理 → 产出 → 回流 变成可执行的日常动作。

## 0. 总体原则

- 知识库是**活的 Wiki**：协作共建、超链接网状、版本留痕、低门槛标记（Markdown）。
- 所有条目可追溯至源头文件；新增 / 修改须同步更新索引。
- **先跑通，再优化**：不预先设计完美体系，每一轮改进都由真实痛点驱动。
- 本规范与现有 `知识库首页.md`、`工作/工作目录说明.md` 配套使用，不替代既有目录结构。

## 0.1 最小闭环工作流（v2.0 新增）

日常只做四件事，形成闭环：

| 步骤 | 动作 | 落点 | 判据 |
|:--|:--|:--|:--|
| ① 采集 | 看到有价值的内容，30 秒记下 | `收件箱/` | 不重命名、不分类、不摘录 |
| ② 处理 | 提炼成可独立理解的知识条目 | 对应分区 | 收件箱 24 小时内清空 |
| ③ 产出 | 形成判断 / 报告 / 汇报 | `产出/` 或 `工作/` | 结论落成文件，不停留在对话里 |
| ④ 回流 | 产出结论反写回知识条目 | 原条目 + 反向链接 | 每条产出至少一条回流链接 |

**每日动作**：打开 [[工作台.md]] → 清空收件箱 → 至少一条产出 → 打勾回流。

**配套工具**：

- 采集：`模板/收件速记.md`、Obsidian Web Clipper、fast-note-sync
- 处理：`模板/知识条目.md`
- 产出：`模板/产出报告.md`
- 日志：`模板/日志模板.md`（日记功能已绑定，`Ctrl+P` → 日记：打开今天的日记）

**三条铁律**：

1. 收件箱里不允许有"半成品"——要么处理完，要么删掉。
2. 答案必须落在文件里，不能只停在聊天窗口。
3. 超过 7 天未动的收件条目，优先做"销毁"判断——留着不处理比删掉更糟。

## 1. 内容标准

- **命名规则**
  - 笔记：`主题` 或 `主题-YYYYMMDD`（如 `WorkBuddy人机双写技巧-20260806.md`）。
  - 合同 / 上会材料：文件名含签约日期或合同编号（沿用现有规则）。
  - 通讯录：`姓名-单位.md`（沿用现有规则）。
  - 禁止：`*`、`?`、`<>`、`/` 等非法字符；禁止 `-1`、`-副本`、`新建文件` 等临时后缀。
- **目录结构**：按现有分区（工作 / 学习 / 工具资源 / 其他）+ 子目录；每个一级目录设 `目录.md` 或 `索引.md` 作为 MOC（Map of Content）。
- **条目粒度**：一条一事、可独立理解；长文拆分子条目并用 `[[ ]]` 互链。
- **总入口**：`知识库首页.md` 为统一导航页，分模块维护。

## 2. 质量标准（强制 frontmatter）

所有笔记头部须含以下字段：

```yaml
---
title: 标题
updated: YYYY-MM-DD
tags: [分类1, 分类2]
status: active | deprecated | archived
source: 溯源文件或链接（可选）
---
```

- 准确：内容与源文件一致；
- 完整：上述要素齐全；
- 时效：`updated` 随改随更；
- 可追溯：`source` 标注出处（对应"所有数据可追溯至源文件"要求）。

## 3. 流程标准

闭环：**创建 → 评审（重要 / 对外内容）→ 发布 → 更新 → 归档**

- 新增笔记后**必须**更新对应目录索引（推荐用 Waypoint 插件自动生成 / 维护 MOC）。
- 修改留痕：Obsidian 内置历史 + Syncthing `.stversions` 已提供文件级版本留痕，无需另建机制。
- 对外发布前须经脱敏与复核。

## 4. 权限与责任

- 当前为个人库：Owner = 本人；多端同步由 Syncthing + remotely-save 承载。
- 分级：公开 / 内部 / 受限（受限内容不进云端同步）。
- 对外共享前须脱敏，敏感字段（身份证号、合同金额、个人联系方式）不进云。

## 5. 生命周期

- **版本**：Syncthing `.stversions` 自动保留旧版（文件级备份）。
- **定期复核**：建议每季度核对 `status`，过期内容标 `deprecated` / `archived`。
- **废止**：制度类归入「旧版废止」目录；其他内容加 `status: deprecated` 并注明替代项，不物理删除。

## 6. 度量改进

- **一键体检**：库根执行 `python 工具/知识库体检.py`，自动重写 `工具/体检报告.md`。即时报表见 [[知识库体检.md]]（Dataview）。每日看板见 [[工作台.md]]。
- **体检项**：断链 `[[ ]]`、孤儿笔记（无入链亦无出链）、空笔记、缺 frontmatter、超 180 天未更新。
- **达标线**：断链 0 条 ｜ frontmatter 完整率 ≥95% ｜ 空笔记 0 篇 ｜ 织网率 ≥20% ｜ 收件箱堆积 ≤7 天。
- **织网率**：正文 `[[ ]]` 互链比例（2026-09-15 基线 84.5%，已达标，继续维持）。
- **定期清理**：同步冲突副本 `*.sync-conflict-*`、过旧 `.stversions` 版本按需清理（清理前先备份）。

## 7. 安全红线

- ⚠️ **密钥 / 凭证不得明文入库**（如 `AI各种PAIKEY重要`）；改用环境变量、密码管理器或库外加密文件。
  - 现状（2026-09-15 复核）：`AI各种PAIKEY重要-1.md` / `.txt` 仍在库根，**已加入 `.gitignore` 故未进 GitHub 历史**，但会随 Syncthing / remotely-save 上云。**建议移出库外**（如 `D:\备份\密钥\`），库内仅留一份索引说明。
  - 同时该文件名带 `-1` 后缀，违反 §1 命名规则，一并处理。
- ⚠️ 含个人敏感信息（身份证、合同金额、通讯录）的内容不进入云端同步。
- ⚠️ `收件箱/` 属"未定级"区域，剪藏的网页可能含个人信息，处理时确认后再进分区。
- 同步冲突副本（`*.sync-conflict-*`）属临时文件，确认无用后清理，清理前备份。

## 8. 推荐插件

| 插件 | 用途 | 状态 |
|:--|:--|:--|
| Templater | 一键生成含 frontmatter 的笔记模板 | ✅ 已启用，模板目录已指向 `模板/` |
| Dataview | 工作台、体检、产出索引报表 | ✅ 已启用（v2.0 起） |
| Waypoint | 自动维护目录 MOC | ⬜ 未装，可选 |
| Linter | 批量规范化格式与 frontmatter | ⬜ 未装，可用 `工具/规范frontmatter.py` 替代 |

> v2.0 说明：Dataview 此前已安装但未启用，本次已加入启用列表；Templater 此前未配置模板目录，本次已绑定 `模板/`，并关闭"启用系统命令"以降低风险。

---

*本规范由 AI 助手依据知识库现状评估生成，纳入库根供遵循；后续可按实际使用情况修订。*

*v2.0（2026-09-15）：引入最小闭环工作流，配套新建 `收件箱/`、`产出/`、`模板/`、`工具/`，新增 [[工作台.md]] 与 [[知识库体检.md]]。*
