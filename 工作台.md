---
title: 🎛️ 工作台
updated: 2026-09-15
tags: [工作台, Dataview, 最小闭环]
status: active
---

# 🎛️ 工作台

> 每天打开这一个页面就够了。上面是入口，下面是"该干的活"。

## 🚀 今日闭环（四步）

| 步骤 | 动作 | 入口 |
|:--|:--|:--|
| ① 采集 | 看到好东西，30 秒丢进收件箱 | [[模板/收件速记.md]] |
| ② 处理 | 清空收件箱，提炼成知识条目 | [[收件箱/待处理清单.md]] |
| ③ 产出 | 写一份判断/报告，落成文件 | [[模板/产出报告.md]] |
| ④ 回流 | 结论反写回知识条目，建反向链接 | [[产出/产出索引.md]] |

---

## 📥 收件箱待处理

> 目标：清空。单条超过 7 天未处理，优先做"销毁"判断。

```dataview
TABLE WITHOUT ID file.link AS "条目", file.ctime AS "放入时间", status AS "状态"
FROM "收件箱"
WHERE file.name != "收件箱说明" AND file.name != "待处理清单"
SORT file.ctime DESC
```

## 📤 最近产出

```dataview
TABLE WITHOUT ID file.link AS "产出", updated AS "更新日", file.folder AS "分类"
FROM "产出"
WHERE file.name != "产出说明" AND file.name != "产出索引"
SORT file.mtime DESC
LIMIT 10
```

---

## 🕐 近 14 天动过的笔记

```dataview
TABLE WITHOUT ID file.link AS "条目", file.folder AS "位置", file.mtime AS "更新"
FROM ""
WHERE file.mtime >= date(today) - dur(14 days)
  AND !startswith(file.folder, "收件箱")
  AND !startswith(file.folder, "产出")
  AND !startswith(file.folder, "模板")
  AND !startswith(file.folder, "工具")
  AND !startswith(file.folder, "copilot")
  AND !startswith(file.folder, "PD2405")
SORT file.mtime DESC
LIMIT 20
```

## ⏳ 超期未更新（180 天以上）

> 每季度过一次：还有用就更新 `updated`，没用了标 `deprecated` 或 `archived`。

```dataview
TABLE WITHOUT ID file.link AS "条目", updated AS "更新日", status AS "状态"
FROM "工作" OR "个人资料" OR "书籍" OR "提示词" OR "证书" OR "通讯录"
WHERE updated < date(today) - dur(180 days)
SORT updated ASC
LIMIT 20
```

---

## ✅ 我的任务

见 [[我的任务.md]]

---

**深入体检**：[[知识库体检.md]] ｜ **规范**：[[白水知识库管理规范.md]]

*最后更新：2026-09-15*
